# Uncomment this line to define a global platform for your project
# platform :ios, '9.0'

target 'boxplatform-photo-tagger' do
  # Uncomment this line if you're using Swift or would like to use dynamic frameworks
  # use_frameworks!

  # Pods for boxplatform-photo-tagger
  pod 'AFNetworking', '~> 2.0'
  pod 'box-ios-sdk'
  pod 'box-ios-preview-sdk'

  target 'boxplatform-photo-taggerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'boxplatform-photo-taggerUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
